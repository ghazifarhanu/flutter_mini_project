import 'package:flutter/material.dart';

class katalog_widget extends StatelessWidget {
  const katalog_widget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(15),
    );
  }
}
